declare namespace NodeJS {
  interface ProcessEnv {
    // just for testing, don't use it in production
    TMT_TENCENT_CLOUD_SECRET_ID: string;
    // just for testing, don't use it in production
    TMT_TENCENT_CLOUD_SECRET_KEY: string;
  }
}
