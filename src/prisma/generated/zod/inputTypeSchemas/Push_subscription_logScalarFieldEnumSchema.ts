import { z } from 'zod';

export const Push_subscription_logScalarFieldEnumSchema = z.enum(['id','pushSubscriptionJSON','endpoint','createdAt','updatedAt','userId']);

export default Push_subscription_logScalarFieldEnumSchema;
